@page
@model ClinicBooking.Web.Pages.BenhNhan.DatLichModel
@{
    ViewData["Title"] = "Đặt lịch khám";
}

<div class="page-header">
    <div>
        <h1 class="page-title">Đặt lịch online</h1>
        <div class="page-subtitle">Chọn ngày đến khám, nhập giờ mong muốn và xác thực OTP trước khi hoàn tất.</div>
    </div>
    <div class="action-bar">
        <a href="/BenhNhan/DanhSachLichHen" class="btn btn-secondary">
            <i class="ph ph-arrow-left"></i> Quay lại lịch hẹn
        </a>
    </div>
</div>

@if (TempData["ErrorMessage"] is string err)
{
    <div class="alert alert-danger">@err</div>
}
@if (TempData["SuccessMessage"] is string success)
{
    <div class="alert alert-success">@success</div>
}

<div class="booking-stepbar card" style="padding: var(--space-4); margin-bottom: var(--space-4); background: linear-gradient(135deg, #eef5ff, #ffffff);">
    <div class="booking-stepbar__label">Đặt lịch khám</div>
    <div class="booking-stepbar__title">Chọn thông tin khám và hoàn tất đặt lịch</div>
    <div class="booking-stepbar__steps">
        <span>1. Chọn thông tin</span>
        <span>2. Chọn giờ khám</span>
        <span>3. Xác thực OTP</span>
    </div>
</div>

<form method="post" class="panel-grid-2">
    <div class="section-stack">
        <div class="card">
            <div class="card-header">
                <span class="card-title"><i class="ph ph-calendar"></i> Thông tin đặt lịch</span>
            </div>
            <div style="padding: var(--space-4);" class="section-stack">
                <div class="form-group">
                    <label class="form-label">Ngày đến khám</label>
                    <input type="date" class="form-input" asp-for="NgayChon"
                           min="@DateOnly.FromDateTime(DateTime.Now.AddDays(1)).ToString("yyyy-MM-dd")" />
                </div>
                <div class="form-group">
                    <label class="form-label">Giờ mong muốn</label>
                    <input type="time" class="form-input" asp-for="GioMongMuon" />
                    <div class="form-hint">Bạn chọn giờ mong muốn, hệ thống sẽ tự tìm ca khám phù hợp và sắp xếp sau khi đặt lịch.</div>
                </div>
                <div class="form-group">
                    <label class="form-label">Dịch vụ khám</label>
                    @if (!Model.DanhSachDichVu.Any())
                    {
                        <p style="color:var(--color-text-secondary); padding: var(--space-3);">Chưa có dịch vụ nào.</p>
                    }
                    else
                    {
                        <select class="form-select" asp-for="IdDichVu" style="width:100%;" required>
                            <option value="0">— Chọn dịch vụ —</option>
                            @foreach (var dv in Model.DanhSachDichVu)
                            {
                                <option value="@dv.IdDichVu">@dv.TenDichVu</option>
                            }
                        </select>
                    }
                </div>
                <div class="form-group">
                    <label class="form-label">Lý do khám / triệu chứng</label>
                    <textarea class="form-input" asp-for="TrieuChung" rows="4" placeholder="Mệt mỏi, cơn đau, tái khám..."></textarea>
                </div>
                <div class="form-group">
                    <label class="form-label">Ghi chú (nếu có)</label>
                    <textarea class="form-input" asp-for="GhiChu" rows="3" placeholder="Muốn gặp bác sĩ đã khám lần trước..."></textarea>
                </div>
            </div>
        </div>

        <div class="card">
            <div class="card-header">
                <span class="card-title"><i class="ph ph-shield-check"></i> Xác thực</span>
                <span class="badge badge-primary">Bước 3</span>
            </div>
            <div style="padding: var(--space-4);" class="section-stack">
                <div class="form-group">
                    <label class="form-label">Mã OTP</label>
                    <input type="text" class="form-input" asp-for="Otp" maxlength="6" inputmode="numeric" placeholder="Nhập 6 số" />
                </div>
                <div class="filter-actions">
                    <button type="submit" formaction="?handler=GuiOtp" class="btn btn-secondary">Gửi mã OTP</button>
                    <button type="submit" class="btn btn-primary">Hoàn tất đặt lịch</button>
                </div>
                <div class="form-hint">Bấm gửi OTP để tạo mã xác thực, sau đó nhập mã và bấm hoàn tất đặt lịch.</div>
            </div>
        </div>
    </div>

    <div class="section-stack">
        <div class="card">
            <div class="card-header">
                <span class="card-title"><i class="ph ph-info"></i> Thông tin đặt khám</span>
            </div>
            <div class="summary-stack">
                <div class="summary-item">
                    <span class="summary-label">Ngày giờ</span>
                    <span id="summary-gio">@Model.NgayChon.ToString("dd/MM/yyyy") lúc @Model.GioMongMuon.ToString("HH:mm")</span>
                </div>
                <div class="summary-item">
                    <span class="summary-label">Dịch vụ</span>
                    <span id="summary-dichvu">@Model.TenDichVuDaChon</span>
                </div>
                <div class="summary-item">
                    <span class="summary-label">Chuyên khoa</span>
                    <span>@Model.ChuyenKhoaHienTai</span>
                </div>
                <div class="summary-item">
                    <span class="summary-label">Bác sĩ khám</span>
                    <span id="summary-bacsi">Hệ thống sẽ tự sắp xếp phù hợp</span>
                </div>
                <div class="summary-item">
                    <span class="summary-label">Phòng khám</span>
                    <span id="summary-phong">Sẽ cập nhật sau khi đặt lịch</span>
                </div>
            </div>
        </div>

        <div class="action-bar">
            <a href="/BenhNhan/DanhSachLichHen" class="btn btn-secondary">Huỷ bỏ</a>
        </div>

        <div class="card">
            <div class="card-header">
                <span class="card-title"><i class="ph ph-lightbulb"></i> Cách hoạt động</span>
            </div>
            <div style="padding: var(--space-4);" class="form-hint">
                Bạn chỉ cần chọn ngày, nhập giờ mong muốn và gửi yêu cầu. Hệ thống sẽ tự khớp ca làm việc phù hợp, tạo lịch hẹn và thông báo kết quả sau khi xử lý xong.
            </div>
        </div>
    </div>
</form>

@section Scripts {
<script>
const ngayInput = document.querySelector('input[name="NgayChon"]');
const gioInput = document.querySelector('input[name="GioMongMuon"]');
const summaryGio = document.getElementById('summary-gio');
if (ngayInput && gioInput && summaryGio) {
    const renderSummary = () => {
        summaryGio.textContent = `${ngayInput.value ? new Date(ngayInput.value).toLocaleDateString('vi-VN') : ''} lúc ${gioInput.value}`;
    };
    ngayInput.addEventListener('change', renderSummary);
    gioInput.addEventListener('input', renderSummary);
}
</script>
}
